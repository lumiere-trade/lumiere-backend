# Production Environment
ENV=production

# External Ports
COURIER_PORT=8765
POURTIER_PORT=8000
PASSEUR_PORT=8766

# Database (CHANGE IN PRODUCTION!)
POSTGRES_USER=lumiere_prod
POSTGRES_PASSWORD=CHANGE_THIS_IN_PRODUCTION
POSTGRES_DB=lumiere_production

# JWT (CHANGE IN PRODUCTION!)
JWT_SECRET_KEY=CHANGE_THIS_SUPER_SECRET_JWT_KEY

# Solana (CHANGE IN PRODUCTION!)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
SOLANA_NETWORK=mainnet-beta
ESCROW_PROGRAM_ID=YOUR_PRODUCTION_PROGRAM_ID

# Service URLs use Docker DNS (no override needed)
